version: '3.8'
services:
  backend:
    build: 
      context: ..
      dockerfile: Docker/Dockerfile.backend
    ports:
      - "8000:8000"
  frontend:
    build:
      context: ..
      dockerfile: Docker/Dockerfile.frontend
    ports:
      - "3000:3000"
